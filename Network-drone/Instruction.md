# Code for Networked drone in MQTT 
<br>
To install the Python MQTT API on Intel Aero (should be there already) : <br>
```pip install paho-mqtt```  <br>
UNPLUG THE PROPELLERS BEFORE RUNNING THIS CODE.
